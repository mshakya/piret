language: python
sudo: false

python:
  - "2.7"

cache:
  directories:
    - $HOME/PyPiReT/thirdParty/miniconda
    - $HOME/PyPiReT/bin

install:
  - pip install -r requirements.txt
  - pip install codecov  
  - pip install pytest-cov
  - ./INSTALL.sh

script:   
  - sh tests/test_pipeline_linux.sh
  - py.test --cov=./

after_success:
  - codecov