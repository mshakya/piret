language: python
sudo: false

python:
  - "2.7"

cache:
  directories:
    - $HOME/build/mshakya/PyPiReT/thirdParty/miniconda
    - $HOME/build/mshakya/PyPiReT/bin
    - $HOME/build/mshakya/PyPiReT/ext
    - $HOME/.cache/pip

install:
  - pip install luigi
  - pip install plumbum
  - pip install codecov  
  - pip install pytest-cov
  - ./INSTALL.sh

script:   
  - sh tests/test_pipeline_linux.sh
  # - py.test --cov=./

after_success:
  - codecov